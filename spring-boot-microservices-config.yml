# Remember that, if you have set a remote repository, to always also push the commits,
# otherwise the configuration server will overwrite the local repo with its cache.

# Used to test @Value in GreetingController
my:
  greeting: Updated-2 Hello World from 'spring-boot-microservices-config' microservice specific file on Configuration Server